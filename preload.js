// El juego corre como una app web normal servida por el servidor local
// (Express + Socket.IO), así que el renderer no necesita APIs de Node/Electron.
// Se mantiene contextIsolation activo y nodeIntegration desactivado por seguridad.
